apiVersion: v1
kind: ConfigMap
metadata:
  name: kube-rbac-proxy
  namespace: default
data:
  config-file.yaml: |+
    authorization:
      resourceAttributes:
        namespace: default
        apiVersion: v1
        resource: services
        subresource: proxy
        name: kube-rbac-proxy
